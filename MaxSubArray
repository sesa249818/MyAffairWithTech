func Kadane(arr []int) int{
	maxC,maxG:=arr[0],arr[0]
	for i:=1; i< len(arr) -1 ; i ++{
		maxC=max(arr[i],maxC+ arr[i])
		if maxC > maxG {
			maxG = maxC
		}
	}
}
func max (num int, num1 int) m{
	if num > num1{
		return num
	}else{
		return num1
	}
}
